add_subdirectory(External)



set(pythondir ${CMAKE_BINARY_DIR}/python/FastSimDataFormats)

file(MAKE_DIRECTORY ${pythondir})

file(WRITE  "${pythondir}/__init__.py" "#autogenerated
import os
__path__.append(os.path.dirname(os.path.realpath(__file__)))")
# Meta-target that builds everything in this subsystem
add_custom_target(FastSimDataFormats_all)

add_dependencies(FastSimDataFormats_all
  FastSimDataFormatsExternal
)
