if(NOT GCC_CCOMPILER_FOUND)
	mark_as_advanced(GCC_CCOMPILER_FOUND)
	set(GCC_CCOMPILER_FOUND TRUE)
	list(APPEND PROJECT_CFLAGS -O2)
	list(APPEND PROJECT_CFLAGS -pthread)
	string(JOIN " " TMP_CFLAGS ${PROJECT_CFLAGS})
	string(JOIN " "  CMAKE_C_FLAGS ${CMAKE_C_FLAGS} ${TMP_CFLAGS})
endif()
