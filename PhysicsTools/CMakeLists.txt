add_subdirectory(FWLite)

add_subdirectory(Utilities)

add_subdirectory(SelectorUtils)



set(pythondir ${CMAKE_BINARY_DIR}/python/PhysicsTools)

file(MAKE_DIRECTORY ${pythondir})

file(WRITE  "${pythondir}/__init__.py" "#autogenerated
import os
__path__.append(os.path.dirname(os.path.realpath(__file__)))")
# Meta-target that builds everything in this subsystem
add_custom_target(PhysicsTools_all)

add_dependencies(PhysicsTools_all
  PhysicsToolsFWLite
  PhysicsToolsUtilities
  PhysicsToolsSelectorUtils
)
