if(NOT GCC_F77COMPILER_FOUND)
  mark_as_advanced(GCC_F77COMPILER_FOUND)
  set(GCC_F77COMPILER_FOUND TRUE)
  list(APPEND LIBS gfortran)
  list(APPEND LIBS m)
  list(APPEND PROJECT_FFLAGS -fno-second-underscore)
  list(APPEND PROJECT_FFLAGS -Wunused)
  list(APPEND PROJECT_FFLAGS -Wuninitialized)
  list(APPEND PROJECT_FFLAGS -O2)
  list(APPEND PROJECT_FFLAGS -cpp)
  list(APPEND CMAKE_F_FLAGS  ${PROJECT_FFLAGS})
endif()
